import { StyleSheet } from 'react-native';

const ArithmeticButtonsComponentStyles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',
    flexWrap: 'nowrap',
  },
});

export default ArithmeticButtonsComponentStyles;
